{
	"name": "Sonnet Scripts Dev Container",
	"dockerComposeFile": "docker-compose.yml",
	"service": "pythonbase",
	"initializeCommand": ["/bin/sh", "-c", "docker compose build linuxbase && docker compose build pythonbase"],
	"workspaceFolder": "/workspaces/sonnet-scripts",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python"
			]
		}
	},
	"postCreateCommand": [
		"make setup",
		"make install-cli",
		"echo 'Sonnet Scripts dev container setup complete!'"
	]
}
